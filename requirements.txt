# Core app remains mostly standard-library based.
# Optional but recommended for server-side PDF generation:
WeasyPrint>=62.0

# Note: WeasyPrint may require additional system libraries in production
# (for example cairo/pango-related packages depending on deployment target).
